node_modules
/.expo
/.firebase

# Logs / local outputs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
adb_log.txt
make_output.txt

# Local image sets (generated / not for repo)
set*.png

# Local database / server data
/.data
*.sqlite

# Expo prebuild outputs (recommended: keep generated native projects out of git)
/android
/ios

# Build outputs
/build

# Local build/export artifacts
/tmp
/web

# Local env files (never commit)
.env
.env.*

# Signing keys / certs (never commit)
*.key
*.p8
*.pem
*.cer
*.crt
*.mobileprovision
*.keystore
*.jks

# Store/CI credential artifacts (generated locally or in CI)
google-play-service-account.json
appstore_connect.json

# Local Expo web export output (generated)
/dist
/web-dist

# Accidental nested clone of this same repo (avoid confusion)
/BuddyBoard


.env.local
