Tablet View

Therapist path:

Dashboard screen
Add a small, persistent icon (like a plus sign) for logging quick notes, incidents, or unexpected data points outside of a scheduled session between the center of the screen and right margin. It should open a small dropdown list, then when a selection is made, open an overlay module to input the data, then close when submitted.
Move the help button to the far right margin.
Make sure that the nav links in the left panel only show active when they are selected. Right now it seems like dashboard, tap tracker, summary review, reports, and schedule all show active when either one is selected.
Child carousel under the header for the therapist to pick the child for the current session. So when the child is selected, the area below ( the rest of the screen area should be a “Start Session” button ½ the size of the area centered in the middle of the section) should activate (looks darker green when inactive, true green when active) allowing the therapist to click the button.
When the start session is pressed, the session should begin and the user should be directed to the tap tracker screen.
Tap Tracker screen
Display a session timer in the center of the header and move the “End Session” button next to it and change it to a pause icon and add and end icon button to the right of it that not only pause the timer but also pauses the tap tracker buttons from being tapped and tracked until resumed and end opens a confirmation module. If the user presses no, they go back to the in-progress session, if yes, they go to the summary screen to review the session.
Remove the “Open summary review” button from the preview learner card.
The preview learner card should display the child's avatar and other information (name, age, gender, medical conditions, other pertinent info) to the right of the avatar.
Make the button grid more refined with rounded corner square buttons not rectangular. The subtext should begin directly below the label and not skip a row.
Remove the long-press behavior on the buttons, its not functioning at all. When a button is tapped, it should open a menu module in the center of the screen with a grid of button options for the therapist to define the event. Options are in the “Tap Tracker Buttons” tab of this document.
Move recent event feed to its own screen and add “Tap Logs” to the nav panel under tap tracker.
Remove the save note button and input field from the bottom of the page.
Tap Logs screen
This screen should be a basic screen allowing the therapist to remove or edit an accidental event. All edits should also be recorded. If the report has already been completed, the therapist needs to provide a reason why the edit is made. This edit should be sent to admin alerts to be denied, nothing changes, if approved, edits are made in the corresponding report with the change and timestamp noted in notes.
Summary Review screen
Change “Summary Review” to “Session Report” everywhere in the app.
Remove the “Open tap tracker” button and duplicate the preview learner card from the tap tracker screen.
Move the “Draft summary review” section input container down between the summary and the buttons and name it “Notes”.
Remove the “Draft Summary” label.
Remove the “Latest approved session summary” section and save the draft button.
Convert the “Approve Summary” button to a submit button.
Submitting should display a success message, then redirect the therapist back to the Dashboard screen. Reports are sent to the “Reports” screen. (Move “Reports” screen to admin path).
Admin Path - ( BCBA’s and Office personnel will share this path. )

Whole Path
Refer to the adminPermissions.txt and adminNavSplit.txt files for the re-design
For the import/export buttons that were disabled (if can’t be found, recreate and use the supplied icon assets. Also use the following functionality for its system:
Universal Data Import & Auto‑Mapping Engine that allows organizations to import all of their existing data from any external platform, regardless of structure or file type. The importer accepts CSV, JSON, Excel, and Google Sheets exports, then automatically analyzes the dataset to identify patterns, field names, and relationships. Using a built‑in similarity‑matching engine, the system compares incoming fields to the app’s internal schema and intelligently connects equivalent data (e.g., “Learner,” “Student,” and “Child Name” are recognized as the same field). Unknown or ambiguous fields can be manually mapped through a simple UI, and the system remembers these mappings for future imports. This enables seamless onboarding from any third‑party system and ensures that the app can extract, normalize, and use as much of the imported data as possible without requiring a standardized template.
Entire App -

The reset password screen needs a status bar and header added. The battery and clock are overlapping the content.
Remove the back button.
Add a persistent status bar to keep the status bar visible and move the header directly under it.
Remove the iPad layout card from the screen.
Move the titleLogo.png to the left margin of the header.
Place a user greeting between the titleLogo.png and the center of the screen.